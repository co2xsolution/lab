/**
 * LabChem — the only file you need to edit.
 *
 * Paste the Apps Script web app URL below. Get it from the Apps Script editor:
 *   Deploy → Manage deployments → copy the Web app URL.
 * It ends in /exec — not /dev, which only works for you.
 */
window.LABCHEM_CONFIG = {
  apiUrl: 'PASTE_YOUR_APPS_SCRIPT_EXEC_URL_HERE'
};
